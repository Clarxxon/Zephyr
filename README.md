# Zephyr

## 🚀 Запуск проекта в Bash с виртуальным окружением

Ниже приведена последовательность команд для настройки и запуска проекта из командной строки (`bash`). Все команды выполняются из корня репозитория.

1. **Создать виртуальное окружение**
   ```bash
   python3 -m venv .venv
   ```
2. **Активировать его**
   ```bash
   source .venv/bin/activate
   ```
3. **Установить зависимости**
   ```bash
   pip install -r requirements.txt
   ```
4. **Запустить сервер** (пример):
   Запуск напрямую из файла может давать ошибку `ModuleNotFoundError`, потому что Python добавляет путь к скрипту в начало `sys.path`. Рекомендуется выполнять модуль как:
   ```bash
   python -m mini_messenger.server_run
   ```
   или, если всё же хочется запустить скрипт напрямую, убедитесь, что текущая директория (`.`) находится в `PYTHONPATH`:
   ```bash
   PYTHONPATH=. python mini_messenger/server_run.py
   ```

   После добавления `__init__.py` в корень пакета и правильной установки `PYTHONPATH` оба способа будут работать.
5. **Запустить клиент** (в другом терминале, виртуальное окружение должно быть активировано):
   ```bash
   python mini_messenger/client_run.py
   ```

---

Дополнительные инструкции и параметры могут находиться в исходных файлах. После завершения работы окружение можно деактивировать с помощью команды `deactivate`.

